import React from 'react';
import { Repositories as LuminaRepositories } from '../../lumina-edition/components/Repositories';

export const LuminaRepositoriesView: React.FC = () => {
  // Casca visual do Lumina para repos; os links ja apontam para os repos corretos.
  return <LuminaRepositories />;
};

